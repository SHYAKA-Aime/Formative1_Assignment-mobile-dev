import 'package:flutter/material.dart';

class CampusBadge extends StatelessWidget {
  const CampusBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
